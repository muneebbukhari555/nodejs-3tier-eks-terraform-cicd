# Build Guide → moved

This content has been consolidated into the single deployment runbook:

➡️ **[`../DEPLOYMENT.md`](../DEPLOYMENT.md)**

It covers, in order: bootstrap (state + OIDC + roles), infrastructure
(Terraform module wiring), application deploy, Ingress (ALB), CDN (CloudFront),
and the automated pipelines.
